<div class="panel mb ptm pbm">
  <h5><%= t(:attach_files) %></h5>

  <div data-attachments>

    <%= f.fields_for :attachments, f.object.attachments do |ff| %>
      <%= ff.check_box :_destroy, label: t(:remove, default: 'Remove') + ' - ' + (ff.object.try(:file_file_name) || t(:file_not_found)) %>
    <% end %>

  </div>

  <% params = { reply: true } if f.object.is_a?(Reply) %>
  <% params = { article: true } if f.object.is_a?(Article) %>

  <%= link_to t(:attach_files), new_attachment_path(params), remote: true, class: 'small button radius secondary' %>

</div>
